---
import { ChevronDown } from 'lucide-astro';
---

<div
	id="featured-gallery-scroll"
	class="absolute bottom-10 left-0 right-0 flex justify-center transition-opacity duration-300"
>
	<button
		onclick="scrollToContent();"
		class="flex flex-col items-center text-white/60 hover:text-white transition-colors hover:cursor-pointer"
		aria-label="Desplázate para ver el portafolio"
	>
		<span class="text-sm mb-2 text-white/70">Ver Portafolio</span>
		<ChevronDown size={24} color="currentColor" />
	</button>
</div>

<script is:inline>
	const scrollToContent = () => {
		window.scrollTo({
			top: window.innerHeight,
			behavior: 'smooth',
		});
	};

	document.addEventListener('DOMContentLoaded', () => {
		const button = document.getElementById('featured-gallery-scroll');

		window.addEventListener('scroll', () => {
			if (window.scrollY > 100) {
				button.style.opacity = 0;
				button.style.visibility = 'hidden';
			} else {
				button.style.visibility = 'visible';
				button.style.opacity = 1;
			}
		});
	});
</script>
